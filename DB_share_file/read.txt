# mysqldump 사용해서 백업함

# 22.10.13까지 작업됨 

# 백업 명령어
mysqldump -u [사용자 계정] -p [백업하고자 하는 DB 이름] > [파일명].sql
# 복원 명령어 
mysql -u [사용자 계정] -p [복원될 DB 이름] < [파일명].sql

# 파일에서 한글은 깨져있지만 에디터에서 볼 때만 그런 것이고 DB에 넣고 보면 문제 없음
# 유니코드 인코딩을 하면 에디터나 브라우저 상에서도 잘 보일지도
