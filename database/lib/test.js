let db = require('./db');
let template = require('./template_test');
let url = require('url');
let qs = require('querystring');
const { request } = require('http');

function a(request, response){
  template.query(`SELECT`)
}
