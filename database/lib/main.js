let http = require('http');
let url = require('url');
let qs = require('querystring');
let template = require('./lib/template_test.js');

let app = http.createServer((request, response) => {
  let _url = request.url;
});
// 진행중